{
    "$schema": "https://opencode.ai/config.json",
    "mcp": {
        "playwright": {
            "type": "local",
            "command": ["npx", "-y", "@playwright/mcp", "--browser", "chromium", "--isolated", "--output-dir", ".playwright"],
            "enabled": true
        }
    }
}
