# .env.example
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to git!

# API Keys (example)
# VITE_API_KEY=your_api_key_here
# VITE_API_URL=https://api.example.com

# Database URLs (example)
# VITE_DATABASE_URL=your_database_url_here

# Other sensitive configuration
# VITE_SECRET_TOKEN=your_secret_token_here

# Note: All environment variables exposed to the client must start with VITE_
# Server-side secrets should never use the VITE_ prefix
